name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto-merge patch/minor updates
    if: github.actor == 'dependabot[bot]'
    uses: dinglebear-ai/workflows/.github/workflows/dependabot-automerge.yml@66e64b9f31de7ac1f9aa8c9f87ede9bbec5eae1d
