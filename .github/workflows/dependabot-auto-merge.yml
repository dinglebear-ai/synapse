name: Dependabot Auto-merge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    name: Auto-merge patch/minor updates
    if: github.actor == 'dependabot[bot]'
    uses: dinglebear-ai/workflows/.github/workflows/dependabot-automerge.yml@eb5f36cf517ffc949aca1c81e2ccbc71b5a85267
